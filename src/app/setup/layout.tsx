/**
 * Setup page layout — clean, no sidebar.
 * The wizard needs a full-screen layout without the app navigation.
 */
export default function SetupLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
